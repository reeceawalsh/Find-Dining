import { fireEvent, render, screen } from "@testing-library/react";
import TextInputLabel from "@component/components/FormElements/TextInputLabel";

describe("<TextInputLabel />", () => {
    it("Renders", () => {
        render(<TextInputLabel />);
    });
});
