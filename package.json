{
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "scripts": {
    "testDev": "concurrently \"cd frontend && npm i && npm run dev\" \"cd backend && npm i && npm run develop\"",
    "testBuild": "concurrently \"cd frontend && npm i && npm run build\" \"cd backend && npm i && npm run build\""
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
